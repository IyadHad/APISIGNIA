  "name": "my-api",
  "version": "1.0.0",
  "description": "A Flask API using OpenCV, MediaPipe, and TensorFlow for sign language recognition.",
  "scripts": {
    "start": "vercel dev"
  },
  "dependencies": {
    "vercel": "^28.10.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
}
